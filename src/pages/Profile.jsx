import React from "react";

import { Profile as UserProfile } from "../components";

function Profile() {
    return (
        <div>
            <UserProfile />
        </div>
    );
}

export default Profile;
